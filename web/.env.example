# Optional in local dev because Vite proxies API routes to the backend.
# Set this in Vercel if you want the frontend to call Railway directly instead of
# using web/vercel.json rewrites.
VITE_API_BASE_URL=https://hackhome-hack-production.up.railway.app
